# Copie para .env e ajuste os valores. O .env real NAO deve ser commitado.
# Usado pelo docker-compose para as credenciais do Postgres local.
POSTGRES_DB=treinotracker
POSTGRES_USER=treino
POSTGRES_PASSWORD=troque-esta-senha
